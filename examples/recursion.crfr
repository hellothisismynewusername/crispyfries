fn fact inp i32 : i32 {
    do
        ~inp 1 i32 == if
            1 i32 -> ret
        else
            ~inp ~inp 1 i32 - fact * -> ret
        endif
        -1 i32 -> ret
    ;
}

fn main : i32 {
    do
        4 i32 fact -> ret
    ;
}